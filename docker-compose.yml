services:
  mock-lipila:
    build:
      context: .
    ports:
      - "0.0.0.0:9991:8080"
    volumes:
      - lipila-data:/app/data
    restart: unless-stopped
    environment:
      PORT: "8080"
      DB_PATH: /app/data/lipila.db

volumes:
  lipila-data:
